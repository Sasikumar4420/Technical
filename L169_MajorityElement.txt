class Solution {
    public int majorityElement(int[] nums) {
        int count = 0;
        Integer a = null;
        for (int i : nums) {
            if (count == 0) {
                a = nums;
            }
            count++;

        }
    }
}